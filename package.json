{
  "name": "constitutional-watch-dashboard",
  "version": "0.2.0",
  "private": true,
  "description": "Node-backed deployment for the constitutional watch dashboard",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "check": "node --check storage.js && node --check server.js && node --check app.js && node --check data.js"
  },
  "dependencies": {
    "@simplewebauthn/server": "^13.3.0",
    "pg": "^8.20.0"
  }
}
